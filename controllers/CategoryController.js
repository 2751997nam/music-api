import DB from '../libs/DB.js';

const mangaByCategory = async (request, response) => {
    let filter = request.query;
    let result = {
        status: 'successful',
        result: []
    };

    

    return response.json(response);
}